#include "../circular_lists.h"

int countNodes(Node *head) {
    //si no hay cabeza
        return 0;
    int count = 1;
    Node *temp = head->next;
    while (/*temp no sea cabeza*/) {
        count++;
        //temp pasa al siguiente nodo
    }
    return /*el numero de nodos*/;
}